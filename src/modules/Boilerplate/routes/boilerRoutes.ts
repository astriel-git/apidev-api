// src/modules/Users/routes/userRoutes.ts
// import * as express from 'express';
// import * as boilerController from '../controllers/boilerControllers.ts';
// import * as boilerSchemas from '../validation/boilerSchemas.ts';
// import * as BoilerInterface from '../types/userTypes.ts';
// import { validateRequest } from '../../../core/middlewares/validateRequest.ts';
// // import { authenticateToken } from '../../../core/middlewares/authMiddleware.ts';

// const router = express.Router();
// router.post('/routename', validateRequest<BoilerInterface.WhateverFunctionRequest>(boilerSchemas.boilerSchemaForThisRequest, 'body'), boilerController.controllerFunctioName);

// export default router;
